<Window x:Class="SilkShield_New.View.New_Inventory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="New_Inventory" Height="700" Width="847">

    <Grid>
        <!-- Title -->
        <TextBlock HorizontalAlignment="Left"
                   Margin="28,22,0,0"
                   TextWrapping="Wrap"
                   Text="New Inventory"
                   VerticalAlignment="Top"
                   Height="52"
                   Width="230"
                   FontSize="32"
                   FontWeight="Bold"
                   FontFamily="Dubai Medium"/>

        <!-- Card background -->
        <Rectangle HorizontalAlignment="Left"
        Height="544"
        Margin="28,94,0,0"
        Stroke="DarkGray"
        VerticalAlignment="Top"
        Width="758"
        RadiusX="20"
        RadiusY="20"/>
        <!-- Section Title -->
        <TextBlock HorizontalAlignment="Left"
                   Margin="58,119,0,0"
                   TextWrapping="Wrap"
                   Text="Item Information"
                   VerticalAlignment="Top"
                   Height="28"
                   Width="200"
                   FontSize="20"
                   FontFamily="Arial" Foreground="#FF292727"/>

        <!-- Item Name Label -->
        <TextBlock HorizontalAlignment="Left"
                   Height="24"
                   Margin="79,171,0,0"
                   TextWrapping="Wrap"
                   Text="Item Name"
                   VerticalAlignment="Top"
                   Width="100"
                   FontWeight="Bold"
                   FontSize="16"/>

        <!-- Item Name Input with Placeholder -->
        <Grid Margin="77,210,0,0" Height="38" HorizontalAlignment="Left" Width="200" VerticalAlignment="Top">
            <Border 
                CornerRadius="10"
                BorderBrush="DarkGray"
                BorderThickness="1"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
                <Grid>
                    <!-- TextBox -->
                    <TextBox x:Name="ItemName"
                         Text="{Binding ItemName, UpdateSourceTrigger=PropertyChanged}"
                         Background="Transparent"
                         BorderThickness="0"
                         VerticalContentAlignment="Center"
                         Padding="8,0,0,0"
                         FontSize="14" Margin="0,-2,0,2"/>

                    <!-- Placeholder -->
                    <TextBlock Text="Input Name"
                           Foreground="Gray"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Margin="12,0,0,0"
                           IsHitTestVisible="False" FontSize="13">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <!-- Show placeholder only when empty -->
                                    <DataTrigger Binding="{Binding Text.Length, ElementName=ItemName}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

            </Border>
        </Grid>

        <TextBlock HorizontalAlignment="Left"
            Height="21"
            Margin="77,272,0,0"
            TextWrapping="Wrap"
            Text="Catagory"
            VerticalAlignment="Top"
            Width="100"
            FontWeight="Bold"
            FontSize="16"/>

        <!--category textbox and placeholder-->
        <Grid Margin="77,310,0,0" Height="38" HorizontalAlignment="Left" Width="200" VerticalAlignment="Top">
            <Border 
         CornerRadius="10"
         BorderBrush="DarkGray"
         BorderThickness="1"
         HorizontalAlignment="Stretch"
         VerticalAlignment="Stretch">

                <Grid>
                    <!-- ComboBox -->
                    <ComboBox x:Name="Category" 
                              Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}" 
                              Background="Transparent" 
                              BorderThickness="0" 
                              VerticalContentAlignment="Center" 
                              Padding="8,0,0,0" FontSize="14" 
                              IsEditable="True" Margin="5,2,7,3" 
                              Grid.ColumnSpan="2">

                        <ComboBoxItem>Fabric</ComboBoxItem>
                        <ComboBoxItem>Blinds</ComboBoxItem>
                        <ComboBoxItem>Accessories</ComboBoxItem>
                    </ComboBox>


                    <!-- Placeholder -->
                    <TextBlock Text="Select Category"
                       Foreground="Gray"
                       FontSize="14"
                       Padding="8,0,0,0"
                       IsHitTestVisible="False"
                       VerticalAlignment="Center"
                       Margin="5,2,7,3">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <!-- Show placeholder only when ComboBox text is empty -->
                                    <DataTrigger Binding="{Binding Text.Length, ElementName=Category}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

            </Border>
        </Grid>

        <TextBlock HorizontalAlignment="Left"
            Height="18"
            Margin="77,374,0,0"
            TextWrapping="Wrap"
            Text="Material"
            VerticalAlignment="Top"
            Width="100"
            FontWeight="Bold"
            FontSize="16"/>

        <!--Material textbox and placeholder-->
        <Grid Margin="77,412,0,0" Height="38" HorizontalAlignment="Left" Width="200" VerticalAlignment="Top">

            <Border 
             CornerRadius="10"
             BorderBrush="DarkGray"
             BorderThickness="1"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

                <Grid>
                    <!-- TextBox -->
                    <TextBox x:Name="Material"
                    Text="{Binding Material, UpdateSourceTrigger=PropertyChanged}"
                    Background="Transparent"
                    BorderThickness="0"
                    VerticalContentAlignment="Center"
                    Padding="8,0,0,0"
                    FontSize="14" Width="198" />

                    <!-- Placeholder -->
                    <TextBlock Text="Material"
                    Foreground="Gray"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Margin="11,0,0,0" FontSize="13"
                    >
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <!-- Show placeholder only when empty -->
                                    <DataTrigger Binding="{Binding Text.Length, ElementName=Material}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

            </Border>
        </Grid>
        <!--color code name box-->
        <TextBlock HorizontalAlignment="Left"
            Height="28"
            Margin="81,487,0,0"
            TextWrapping="Wrap"
            Text="Color Code"
            VerticalAlignment="Top"
            Width="96"
            FontSize="16" FontWeight="Bold"/>


        <!--Measuring Unit textbox and placeholder-->
        <Grid Margin="430,210,0,223" Height="38" HorizontalAlignment="Left" Width="200" VerticalAlignment="Top">
            <Border 
             CornerRadius="10"
             BorderBrush="DarkGray"
             BorderThickness="1"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
            >
                <Grid>
                    <ComboBox x:Name="MeasuringUnit" 
                              Text="{Binding MeasuringUnit, UpdateSourceTrigger=PropertyChanged}" 
                              Background="Transparent" 
                              BorderThickness="0" 
                              VerticalContentAlignment="Center" 
                              Padding="8,0,0,0" FontSize="14" 
                              IsEditable="True" Margin="5,2,7,3" 
                              Grid.ColumnSpan="2">

                        <ComboBoxItem>Meters (m)</ComboBoxItem>
                        <ComboBoxItem>Square Meters (sqm)</ComboBoxItem>
                        <ComboBoxItem>Feet (ft)</ComboBoxItem>
                        <ComboBoxItem>Pieces</ComboBoxItem>
                    </ComboBox>

                    <!-- Placeholder TextBlock -->
                    <TextBlock Text="Select Measuring Unit"
                       Foreground="Gray"
                       FontSize="14"
                       Padding="8,0,0,0"
                       IsHitTestVisible="False"
                       VerticalAlignment="Center"
                       Margin="5,2,7,3">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <!-- Show placeholder only when ComboBox text is empty -->
                                    <DataTrigger Binding="{Binding Text.Length, ElementName=MeasuringUnit}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>
        </Grid>
        <TextBlock HorizontalAlignment="Left"
            Height="28"
            Margin="432,175,0,0"
            TextWrapping="Wrap"
            Text="Measuring Unit"
            VerticalAlignment="Top"
            Width="135"
            FontWeight="Bold"
            FontSize="16" RenderTransformOrigin="0.222,0.555"/>

        <Grid Margin="77,520,0,0" Height="38" HorizontalAlignment="Left" Width="200" VerticalAlignment="Top">
            <Border 
        CornerRadius="10"
        BorderBrush="DarkGray"
        BorderThickness="1"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
                <Grid>
                    <!-- TextBox -->
                    <TextBox x:Name="ColorCode"
                 Text="{Binding ColorCode, UpdateSourceTrigger=PropertyChanged}"
                 Background="Transparent"
                 BorderThickness="0"
                 VerticalContentAlignment="Center"
                 Padding="8,0,0,0"
                 FontSize="14" Margin="0,-2,0,2"/>

                    <!-- Placeholder -->
                    <TextBlock Text="Enter the Color Code"
                   Foreground="Gray"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   Margin="10,0,0,0"
                   IsHitTestVisible="False" FontSize="13">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <!-- Show placeholder only when empty -->
                                    <DataTrigger Binding="{Binding Text.Length, ElementName=ColorCode}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

            </Border>
        </Grid>

        <Rectangle HorizontalAlignment="Left" Height="256" Margin="409,271,0,0" Stroke="DarkGray" VerticalAlignment="Top" Width="271" RadiusX="20" RadiusY="20"/>

        <TextBlock HorizontalAlignment="Left"
            Margin="465,287,0,0"
            TextWrapping="Wrap"
            Text="Pricing &amp; Stock"
            VerticalAlignment="Top"
            Height="28"
            Width="149"
            FontSize="20"
            FontFamily="Arial" Foreground="#FF292727"/>
        <TextBlock HorizontalAlignment="Left"
            Height="28"
            Margin="430,336,0,0"
            TextWrapping="Wrap"
            Text="Unit Price(LKR)"
            VerticalAlignment="Top"
            Width="135"
            FontWeight="Bold"
            FontSize="16" RenderTransformOrigin="0.222,0.555"/>

        <Grid Margin="430,370,0,0" Height="38" HorizontalAlignment="Left" Width="200" VerticalAlignment="Top">
            <Border 
            CornerRadius="10"
            BorderBrush="DarkGray"
            BorderThickness="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">
                <Grid>
                    <!-- TextBox -->
                    <TextBox x:Name="Price"
         Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"
         Background="Transparent"
         BorderThickness="0"
         VerticalContentAlignment="Center"
         Padding="8,0,0,0"
         FontSize="14" Margin="0,-2,0,2"/>

                    <!-- Placeholder -->
                    <TextBlock Text="Input Price"
                       Foreground="Gray"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="10,0,0,0"
                       IsHitTestVisible="False" FontSize="13">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <!-- Show placeholder only when empty -->
                                    <DataTrigger Binding="{Binding Text.Length, ElementName=Price}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

            </Border>
        </Grid>
        <TextBlock HorizontalAlignment="Left"
            Height="28"
            Margin="432,431,0,0"
            TextWrapping="Wrap"
            Text="Stock"
            VerticalAlignment="Top"
            Width="135"
            FontWeight="Bold"
            FontSize="16" RenderTransformOrigin="0.222,0.555"/>

        <Grid Margin="430,460,0,0" Height="38" HorizontalAlignment="Left" Width="200" VerticalAlignment="Top">
            <Border 
                CornerRadius="10"
                BorderBrush="DarkGray"
                BorderThickness="1"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
                <Grid>
                    <!-- TextBox -->
                    <ComboBox x:Name="Stock"
                                 Text="{Binding Stock, UpdateSourceTrigger=PropertyChanged}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 VerticalContentAlignment="Center"
                                 Padding="8,0,0,0"
                                 FontSize="14"
                                 IsEditable="True" Margin="5,5,5,4">

                        <ComboBoxItem>In Stock</ComboBoxItem>
                        <ComboBoxItem>Out of Stock</ComboBoxItem>
                    </ComboBox>

                    <!-- Placeholder -->
                    <TextBlock Text="Select Status"
                       Foreground="Gray"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="10,0,0,0"
                       IsHitTestVisible="False" FontSize="13">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <!-- Show placeholder only when empty -->
                                    <DataTrigger Binding="{Binding Text.Length, ElementName=Stock}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

            </Border>
        </Grid>
        <Grid>
         <Button x:Name="btnsave" Content="SAVE" Background="#FF5B281C" Foreground="White" Margin="409,568,336,84" FontSize="15" FontFamily="Segoe UI"
                FontWeight="SemiBold">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                    CornerRadius="12" 
                    BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button x:Name="btnclear" Content="CLEAR" Background="#FF5B281C" Foreground="White" Margin="530,568,215,84" FontSize="15" FontFamily="Segoe UI"
            FontWeight="SemiBold">
            <Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" 
                        CornerRadius="12" 
                        BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center" 
                            VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button x:Name="btncancel" Content="CANCEL" Background="#FF5B281C" Foreground="White" Margin="649,568,96,84" FontSize="15" FontFamily="Segoe UI"
            FontWeight="SemiBold">
            <Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" 
                        CornerRadius="12" 
                        BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center" 
                            VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
          </Button>
       </Grid>







    </Grid>
</Window>